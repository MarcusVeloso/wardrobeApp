<%= form_with(model: calcado) do |form| %>
  <% if calcado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calcado.errors.count, "error") %> prohibited this calcado from being saved:</h2>

      <ul>
        <% calcado.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :tipo_calcado %>
    <%= form.text_field :tipo_calcado %>
  </div>

  <div class="field">
    <%= form.label :tamanho %>
    <%= form.text_field :tamanho %>
  </div>

  <div class="field">
    <%= form.label :marca %>
    <%= form.text_field :marca %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
